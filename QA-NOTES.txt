V4.2 QA UPDATE
- Added keyboard-accessible upload/drop zones.
- Added explicit JPG/PNG/WebP output selection to Image Resizer.
- Added WebP output fallback error handling.

PROJECT TOOLS QA NOTES
=======================
Static JavaScript syntax check: PASSED with Node.js.
Tool pages: present for all six launch tools.
Legal pages: About, Contact, Privacy, Terms, Disclaimer present.
SEO templates: canonical/OG/sitemap placeholders intentionally await final domain.
Important manual launch tests still required:
- Upload and download each supported format on Android Chrome and desktop Chrome/Firefox.
- Test very large images and low-memory devices.
- Verify jsPDF CDN availability in the production hosting environment.
- Replace placeholder domain and contact email.
\nV4.3.1 IMAGE → PDF FIX\n- jsPDF is loaded on demand with jsDelivr + cdnjs fallback instead of relying on one CDN at page load.\n- JPG/PNG input is rasterized to a bounded JPEG canvas before jsPDF insertion for better mobile-browser compatibility.\n- PDF button is disabled while generation runs and restores automatically after success/failure.\n